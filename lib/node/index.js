const paths = require("./paths");
const webpack = require("./webpack");
const mergeDirectories = require("./mergeDirectories");
const writeJsonFile = require("./writeJsonFile");

module.exports = {
  paths,
  webpack,
  mergeDirectories,
  writeJsonFile
};
