const paths = require("./lib/paths");
const webpack = require("./lib/webpack");
const mergeDirectories = require("./lib/mergeDirectories");

module.exports = {
  paths,
  webpack,
  mergeDirectories
};
