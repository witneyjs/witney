#!/usr/bin/env node
<% if (!input.isNode) { -%>

const { paths } = require("../../lib/node");

<% } -%>
require("../templates/build")({
  nameSpaceId: "<%= input.id %>"<% if (!input.isNode) { %>,
  useDevServer: true,
  useBundleAnalyzer: true<% if (!input.isLibrary) { %>,
  rawJsBundles: {
    "externals.js": {
      files: [
        {
          jsPath: paths.project("node_modules/faye/client/faye-browser.js")
        }
      ]
    }
  }<% } %><% } %>
});
